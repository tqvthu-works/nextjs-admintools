// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
model User {
  id                   BigInt                 @id @default(autoincrement()) @db.UnsignedBigInt
  name                 String                 @db.VarChar(255)
  email                String                 @unique(map: "users_email_unique") @db.VarChar(255)
  email_verified_at    DateTime?              @db.Timestamp(0)
  password             String                 @db.VarChar(255)
  status               Boolean                @default(true)
  remember_token       String?                @db.VarChar(100)
  created_at           DateTime?              @db.Timestamp(0)
  updated_at           DateTime?              @db.Timestamp(0)
  @@map("users")
}
model Shop {
  id                                 BigInt                               @id @db.UnsignedBigInt
  name                               String?                              @db.VarChar(255)
  shopify_domain                     String                               @db.VarChar(255)
  domain                             String?                              @db.VarChar(255)
  owner                              String?                              @db.VarChar(255)
  email                              String?                              @db.VarChar(255)
  phone                              String?                              @db.VarChar(255)
  timezone                           String?                              @db.VarChar(255)
  country                            String?                              @db.VarChar(30)
  currency                           String?                              @db.VarChar(30)
  access_token                       String?                              @db.VarChar(255)
  created_at                         DateTime?                            @db.Timestamp(0)
  updated_at                         DateTime?                            @db.Timestamp(0)
  theme_id                           Int?                                 @db.UnsignedInt
  v2_theme_id                        Int?                                 @db.UnsignedInt
  storefront_access_token            String?                              @db.VarChar(255)
  money_format                       String?                              @db.VarChar(255)
  money_with_currency_format         String?                              @db.VarChar(255)
  uninstalled_at                     DateTime?                            @db.DateTime(0)
  plan_name                          String?                              @db.VarChar(100)
  status                             Int                                  @default(1) @db.UnsignedTinyInt
  used_v3                            Boolean                              @default(false)
  is_expert                          Boolean                              @default(false)
  limit_blocks_by_plan               Boolean                              @default(false)
  mobile_auth                        Boolean                              @default(false)
  started_free_trial                 Boolean                              @default(false)
  language                           String?                              @default("en") @db.VarChar(100)
  is_test                            Boolean                              @default(false)
  is_onboarding                      Boolean                              @default(false)
  publish_changes                    Boolean                              @default(false)
  in_app_notify                      Boolean                              @default(true)
  note                               String?                              @db.Text
  subscription                       Subscription? 
  onboarding                          Onboarding?
  @@map("shops")
}

model IntegrationShop {
  id             BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  integration_id Int
  shop_id        BigInt    @db.UnsignedBigInt
  status         String    @default("0") @db.VarChar(255)
  created_at     DateTime? @db.Timestamp(0)
  updated_at     DateTime? @db.Timestamp(0)
  meta           Json
  @@map("integration_shop")
}


model Subscription {
  id            Int       @id @default(autoincrement()) @db.UnsignedInt
  shop_id       BigInt    @db.UnsignedBigInt @unique
  name          String    @db.VarChar(250)
  price         Int       @db.UnsignedInt
  activated_on  DateTime? @db.Date
  billing_on    DateTime? @db.Date
  cancelled_on  DateTime? @db.Date
  trial_ends_on DateTime? @db.Date
  interval      String    @db.VarChar(100)
  paused_at     DateTime? @db.Timestamp(0)
  created_at    DateTime? @db.Timestamp(0)
  updated_at    DateTime? @db.Timestamp(0)
  charge_id     BigInt    @db.UnsignedBigInt
  plan          String    @db.VarChar(100)
  shop         Shop     @relation(fields: [shop_id], references: [id])
  @@map("subscription")
}
model Onboarding {
  id                 BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  shop_id            BigInt    @db.UnsignedBigInt @unique
  select_theme       Boolean   @default(false)
  theme_id           Int?
  setup_branding     Boolean   @default(false)
  design_mobile_app  Boolean   @default(false)
  preview_mobile_app Boolean   @default(false)
  app_submission     Boolean   @default(false)
  dismiss_guide      Boolean   @default(false)
  created_at         DateTime? @db.Timestamp(0)
  updated_at         DateTime? @db.Timestamp(0)
  shop              Shop      @relation(fields: [shop_id], references: [id])
  @@map("onboarding_v2")
}